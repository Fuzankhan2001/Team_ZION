-- Migration 002: Add network tables
-- Phase 3: network messages + referral log tables

ALTER TABLE public.hospital_state
    DROP CONSTRAINT IF EXISTS fk_state_hospital;

ALTER TABLE public.hospital_state
    ADD CONSTRAINT fk_state_hospital
    FOREIGN KEY (facility_id)
    REFERENCES public.hospitals (facility_id);
